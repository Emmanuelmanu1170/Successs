<!DOCTYPE html>
<html>
<head>
  <title>Customer Dashboard - Kwabenamedia</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Montserrat', sans-serif;
      background: url('https://images.pexels.com/photos/415829/pexels-photo-415829.jpeg') no-repeat center center fixed;
      background-size: cover;
      padding: 20px;
      min-height: 100vh;
      display: flex;
      align-items: flex-start;
      justify-content: center;
      position: relative;
      overflow-x: hidden;
      color: #222;
    }

    /* Background overlay */
    body::before {
      content: "";
      position: fixed;
      top: 0; left: 0; right: 0; bottom: 0;
      background: rgba(0,0,0,0.3);
      z-index: -1;
    }

    /* Floating stickers */
    .sticker {
      position: absolute;
      width: 70px;
      opacity: 0.85;
      animation: float 6s ease-in-out infinite;
    }
    .sticker1 { top: 12%; left: 5%; animation-delay: 0s; }
    .sticker2 { bottom: 10%; right: 12%; animation-delay: 2s; }
    .sticker3 { top: 18%; right: 20%; animation-delay: 4s; }
    .sticker4 { bottom: 15%; left: 15%; animation-delay: 3s; }
    .sticker5 { top: 30%; right: 5%; animation-delay: 5s; }
    .sticker6 { top: 45%; left: 50%; animation-delay: 6s; }

    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-20px); }
    }

    .dashboard {
      width: 100%;
      max-width: 1000px;
      background: rgba(255, 255, 255, 0.96);
      backdrop-filter: blur(14px);
      border-radius: 20px;
      padding: 30px;
      box-shadow: 0 18px 45px rgba(0,0,0,0.25);
      animation: fadeIn 1s ease-in-out;
    }

    /* Banner */
    .banner {
      width: 100%;
      padding: 18px;
      background: url('https://i.ibb.co/7k8BznX/promo-banner.jpg') center/cover no-repeat, 
                  linear-gradient(90deg, #008037cc, #00a84fcc);
      color: white;
      text-align: center;
      font-weight: 700;
      border-radius: 14px;
      margin-bottom: 22px;
      font-size: 20px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.25);
      position: relative;
      overflow: hidden;
      animation: flash 2.5s infinite;
    }
    .banner::after {
      content: "🔥 LIMITED OFFER 🔥";
      position: absolute;
      top: 8px; right: 12px;
      font-size: 13px;
      background: #ff0000;
      padding: 4px 10px;
      border-radius: 6px;
      font-weight: bold;
    }

    .banner img.promo-img {
      width: 100%;
      max-height: 180px;
      border-radius: 12px;
      margin-top: 10px;
      object-fit: cover;
    }

    @keyframes flash {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.7; }
    }

    h2 {
      text-align: center;
      color: #008037;
      margin-bottom: 20px;
      font-size: 30px;
      font-weight: 700;
    }

    .order-box {
      background: #fdfdfd;
      padding: 20px;
      border-radius: 15px;
      border: 1px solid #e6e6e6;
      margin-bottom: 25px;
      box-shadow: inset 0 3px 10px rgba(0,0,0,0.05);
    }
    .order-box h3 {
      margin-bottom: 12px;
      color: #008037;
      font-size: 20px;
      font-weight: 600;
    }
    .order-box p {
      margin: 6px 0;
      font-size: 15px;
      color: #444;
      line-height: 1.5;
    }

    .btn {
      display: inline-block;
      padding: 13px 22px;
      margin: 8px 6px 0 0;
      border-radius: 12px;
      text-decoration: none;
      color: #fff;
      font-weight: 600;
      font-size: 15px;
      transition: all 0.3s ease;
      box-shadow: 0 4px 10px rgba(0,0,0,0.15);
    }
    .btn:hover {
      transform: translateY(-2px) scale(1.05);
      box-shadow: 0 6px 15px rgba(0,0,0,0.25);
    }
    .btn-order { background: #28a745; }
    .btn-order:hover { background: #1e7e34; }
    .btn-logout { background: #dc3545; }
    .btn-logout:hover { background: #a71d2a; }
    .btn-contact { background: #007bff; }
    .btn-contact:hover { background: #0056b3; }

    .about-section {
      margin-top: 30px;
      padding: 25px;
      border-radius: 15px;
      background: #ffffffee;
      box-shadow: 0 6px 25px rgba(0,0,0,0.08);
    }
    .about-section h3 {
      text-align: center;
      margin-bottom: 20px;
      color: #008037;
      font-size: 24px;
      font-weight: 600;
    }
    .about-section p {
      text-align: center;
      margin-bottom: 25px;
      font-size: 15px;
      color: #333;
      line-height: 1.6;
    }
    .testimonials {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 20px;
    }
    .testimonial {
      background: #fafafa;
      border: 1px solid #eee;
      border-radius: 14px;
      padding: 18px;
      text-align: center;
      box-shadow: 0 3px 12px rgba(0,0,0,0.05);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .testimonial:hover {
      transform: translateY(-5px);
      box-shadow: 0 6px 20px rgba(0,0,0,0.15);
    }
    .testimonial img {
      width: 90px;
      height: 90px;
      border-radius: 50%;
      margin-bottom: 12px;
      object-fit: cover;
      border: 3px solid #00803733;
    }
    .testimonial p {
      font-size: 14px;
      color: #444;
      font-style: italic;
      line-height: 1.5;
    }
    .testimonial strong {
      display: block;
      margin-top: 10px;
      color: #008037;
      font-size: 15px;
      font-weight: 600;
    }

    @media (max-width: 768px) {
      .dashboard { padding: 18px; }
      h2 { font-size: 24px; }
      .testimonials { grid-template-columns: 1fr; text-align: center; }
      .sticker { width: 50px; }
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>

<!-- Floating stickers -->
<img src="https://cdn-icons-png.flaticon.com/512/1384/1384060.png" class="sticker sticker1" alt="Instagram">
<img src="https://cdn-icons-png.flaticon.com/512/1384/1384065.png" class="sticker sticker2" alt="Facebook">
<img src="https://cdn-icons-png.flaticon.com/512/1384/1384063.png" class="sticker sticker3" alt="Twitter">
<img src="https://cdn-icons-png.flaticon.com/512/1384/1384055.png" class="sticker sticker4" alt="YouTube">
<img src="https://cdn-icons-png.flaticon.com/512/1384/1384062.png" class="sticker sticker5" alt="TikTok">
<img src="https://cdn-icons-png.flaticon.com/512/733/733585.png" class="sticker sticker6" alt="WhatsApp">

<div class="dashboard">
  <!-- Banner -->
  <div class="banner">
    <img src="https://cdn-icons-png.flaticon.com/512/992/992700.png" style="width:30px;vertical-align:middle;margin-right:8px;">
    🎉 Special Promo: Get 20% Off All Orders This Week Only! 🚀
    <br><br>
    <img src="https://images.pexels.com/photos/3184435/pexels-photo-3184435.jpeg" alt="Promo Girl" class="promo-img">
  </div>

  <h2 id="welcomeText">Welcome to Your Dashboard</h2>

  <!-- Action Buttons -->
  <div style="text-align:center; margin-bottom:25px;">
    <a href="order.html" class="btn btn-order">➕ Place Order</a>
    <a href="https://wa.me/233554263293" class="btn btn-contact">📞 Contact Admin</a>
    <a href="#" class="btn btn-logout" onclick="logout()">🚪 Logout</a>
  </div>

  <!-- Latest Order -->
  <div id="orderDetails" class="order-box">
    <p><strong>Loading your latest order...</strong></p>
  </div>

  <!-- About Us -->
  <div class="about-section">
    <h3>About Us</h3>
    <p>
      Kwabenamedia is your trusted hub for boosting your online presence with real, reliable, and fast social media services. 🌍💚  
      We proudly serve Africans and customers worldwide who want to grow their influence online.
    </p>
    <div class="testimonials">
      <div class="testimonial">
        <img src="https://images.pexels.com/photos/774909/pexels-photo-774909.jpeg" alt="foreign woman">
        <p>“I’m Emma from the UK 🇬🇧. Kwabenamedia is the best platform I’ve used to reach African audiences and beyond. Absolutely reliable!”</p>
        <strong>- Emma</strong>
      </div>
      <div class="testimonial">
        <img src="https://images.pexels.com/photos/415829/pexels-photo-415829.jpeg" alt="foreign woman">
        <p>“Sophia here from Canada 🇨🇦. Thanks to Kwabenamedia, my brand is popular not only in Africa but globally. Highly recommend it!”</p>
        <strong>- Sophia</strong>
      </div>
      <div class="testimonial">
        <img src="https://images.pexels.com/photos/2379005/pexels-photo-2379005.jpeg" alt="foreign man">
        <p>“I’m Lucas from Germany 🇩🇪. Kwabenamedia helped my business connect with African markets while also going international.”</p>
        <strong>- Lucas</strong>
      </div>
    </div>
  </div>
</div>

<script>
  function loadOrder() {
    const latestOrder = JSON.parse(localStorage.getItem('latestOrder'));
    const accountName = localStorage.getItem('accountName');
    const orderBox = document.getElementById('orderDetails');

    if (accountName) {
      document.getElementById('welcomeText').innerText = "👋 Welcome back, " + accountName + "!";
    }

    if (latestOrder) {
      orderBox.innerHTML = `
        <h3>Your Latest Order</h3>
        <p><strong>Platform:</strong> ${latestOrder.platform}</p>
        <p><strong>Service:</strong> ${latestOrder.service}</p>
        <p><strong>Quantity:</strong> ${latestOrder.quantity}</p>
        <p><strong>Price (GHS):</strong> ${latestOrder.priceGHS}</p>
        <p><strong>Price (USD):</strong> ${latestOrder.priceUSD}</p>
        <p><strong>Username/Link:</strong> ${latestOrder.username}</p>
        <p><strong>Email:</strong> ${latestOrder.email}</p>
        <p><strong>Phone:</strong> ${latestOrder.phone}</p>
        <p><strong>Reference:</strong> ${latestOrder.reference}</p>
      `;
    } else {
      orderBox.innerHTML = `<p>No recent orders found. Please place an order.</p>`;
    }
  }

  function logout() {
    localStorage.removeItem('latestOrder');
    localStorage.removeItem('accountName');
    window.location.href = 'index.html';
  }

  loadOrder();
</script>

</body>
</html>
